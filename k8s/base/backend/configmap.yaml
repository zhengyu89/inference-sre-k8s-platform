apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
data:
  PORT: "3000"
  # The redis/ Service resolves the same way in every overlay, so it lives
  # here rather than being redefined per environment.
  REDIS_URL: "redis://redis:6379"