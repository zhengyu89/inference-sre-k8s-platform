apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend
  name: frontend
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: http
  selector:
    app: frontend
  type: ClusterIP
status:
  loadBalancer: {}