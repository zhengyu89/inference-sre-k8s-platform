apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  # External managed Postgres — see backend-db-secret.yaml for why this
  # lives here instead of pulling in ../../base/postgres like dev/stag do.
  - backend-db-secret.yaml
