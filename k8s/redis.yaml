# Ephemeral cache — no PVC. Losing Redis's data on a pod restart is
# acceptable for the counters/short-lived state it's used for here.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: inference-sre
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
          readinessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            exec:
              command: ["redis-cli", "ping"]
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: inference-sre
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
