PORT=3000

# PostgreSQL (local dev, see docker-compose.yml at repo root)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/inference_sre
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=inference_sre

# Redis (local dev, see docker-compose.yml at repo root)
REDIS_URL=redis://localhost:6379
